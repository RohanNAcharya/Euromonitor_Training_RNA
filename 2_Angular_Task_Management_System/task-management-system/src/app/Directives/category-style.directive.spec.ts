import { CategoryStyleDirective } from './category-style.directive';

describe('CategoryStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new CategoryStyleDirective();
    expect(directive).toBeTruthy();
  });
});
